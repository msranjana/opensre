id: 322-priority-prompt-orders-cpu-spike
title: Priority prompt — orders-api CPU spike
intent_class: chat_handoff
input:
  prompt: CPU is spiking to 99% on the orders-api pods
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- 'P0 priority prompt (misc_real_investigation #2) from OpenSRE analytics priority-prompts report.'
- Prompt is sent verbatim; do not normalize typos or mixed language.
turn:
  expected_kind: agent
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: incident_description:orders_cpu_99
  source: llm
response_contract:
  must_contain_any:
  - orders
  - cpu
  - incident
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
tool_actions:
- surface: gather
  tools:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
  expect: not_called
