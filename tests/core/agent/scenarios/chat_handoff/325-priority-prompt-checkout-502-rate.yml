id: 325-priority-prompt-checkout-502-rate
title: Priority prompt — checkout 502 error rate
intent_class: chat_handoff
input:
  prompt: the checkout service is returning 502 errors for 30% of requests
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- 'P0 priority prompt (http_5xx_or_latency_after_deploy #1) from OpenSRE analytics priority-prompts report.'
- Prompt is sent verbatim; do not normalize typos or mixed language.
turn:
  expected_kind: agent
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: incident_description:checkout_502_rate
  source: llm
response_contract:
  must_contain_any:
  - checkout
  - '502'
  - incident
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
tool_actions:
- surface: gather
  tools:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
  expect: not_called
