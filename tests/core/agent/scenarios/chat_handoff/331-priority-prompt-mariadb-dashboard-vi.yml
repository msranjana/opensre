id: 331-priority-prompt-mariadb-dashboard-vi
title: Priority prompt — xem dashboard mariadb (Vietnamese)
intent_class: chat_handoff
input:
  prompt: xem dashboard mariadb
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- 'P0 priority prompt (database_connection_or_query_errors #1) from OpenSRE analytics priority-prompts
  report.'
- Prompt is sent verbatim; do not normalize typos or mixed language.
turn:
  expected_kind: agent
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: database_query:mariadb_dashboard
  source: llm
response_contract:
  must_contain_any:
  - mariadb
  - dashboard
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
tool_actions:
- surface: gather
  tools:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
  expect: not_called
