id: 324-priority-prompt-query-grafana-logs-vi
title: Priority prompt — query_grafana_logs (Vietnamese)
intent_class: complex_shell_prompts
input:
  prompt: query_grafana_logs ấy
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- 'P0 priority prompt (logs_metrics_traces_query #2) from OpenSRE analytics priority-prompts report.'
- Prompt is sent verbatim; do not normalize typos or mixed language.
turn:
  expected_kind: agent
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: logs_query:grafana_logs
  source: llm
response_contract:
  must_contain_any:
  - grafana
  - log
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
tool_actions:
- surface: gather
  tools:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
  expect: not_called
